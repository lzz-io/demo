<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
<generatorConfiguration>

    <!-- 一个数据库一个context,context的子元素必须按照它给出的顺序
       property*,plugin*,commentGenerator?,jdbcConnection,javaTypeResolver?,
       javaModelGenerator,sqlMapGenerator?,javaClientGenerator?,table+
   -->
   <!-- targetRuntime="MyBatis3" 生成XxxMapper.xml的关键 -->
   <!-- targetRuntime="MyBatis3" 生成XxxMapper.xml的关键 -->
   <!-- targetRuntime="MyBatis3" 生成XxxMapper.xml的关键 -->
    <context id="context1" targetRuntime="MyBatis3">
        <!-- 生成的Java文件编码 -->
        <property name="javaFileEncoding" value="UTF-8"/>

        <!--支持分页-->
        <plugin type="org.mybatis.generator.plugins.RowBoundsPlugin"/>
        <!--生成的bean支持可序列化-->
        <plugin type="org.mybatis.generator.plugins.SerializablePlugin"/>
        <!--生成的bean有equals和hashCode实现-->
        <plugin type="org.mybatis.generator.plugins.EqualsHashCodePlugin"/>
        <!--生成的bean有toString实现-->
        <plugin type="org.mybatis.generator.plugins.ToStringPlugin"/>

        <!-- 注释 -->
        <commentGenerator>
            <!-- 是否不生成注释 -->
            <property name="suppressAllComments" value="true"/>
            <!-- 不希望生成的注释中包含时间戳 -->
            <!--<property name="suppressDate" value="true"/>-->
            <!-- 添加 db 表中字段的注释，只有suppressAllComments为false时才生效-->
            <!--<property name="addRemarkComments" value="true"/>-->
        </commentGenerator>

        <jdbcConnection connectionURL="jdbc:mysql://localhost:3306/test" driverClass="com.mysql.cj.jdbc.Driver"
                        userId="root"/>

        <!-- 生成实体类地址 -->
        <javaModelGenerator targetPackage="io.lzz.demo.entity" targetProject="src/main/java"/>

        <!-- 生成XxxMapper.xml文件 -->
        <sqlMapGenerator targetPackage="mapper" targetProject="src/main/resources">
            <property name="enableSubPackages" value="true" />
        </sqlMapGenerator>

        <!-- 生成 XxxMapper.java 接口-->
        <javaClientGenerator targetPackage="io.lzz.demo.mapper" targetProject="src/main/java" type="XMLMAPPER"/>

        <table schema="test" tableName="order"></table>
    </context>
</generatorConfiguration>